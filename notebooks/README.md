# Notebooks

Use this directory for exploratory analysis and narrative assembly.

Guidelines:

- Keep production pipeline logic in `src/` and `scripts/`.
- Use notebooks for exploration, interpretation, and blog-ready visuals/tables.
- Export the final post artifact into `notebooks/publish/`.
